diff --git a/api/lib/types.ts b/api/lib/types.ts
index 3749e34f8..98dc02817 100644
--- a/api/lib/types.ts
+++ b/api/lib/types.ts
@@ -225,6 +225,7 @@ export const ServerResponse = Type.Object({
     api: Type.String(),
     webtak: Type.String(),
     auth: Type.Boolean({ description: 'Once an admin certificate is configured it is not retrivable. This boolean refers to if a certificate is currently loaded' }),
+    oidc_enabled: Type.Optional(Type.Boolean({ description: 'Whether ALB OIDC authentication is enabled' })),
     certificate: Type.Optional(Type.Object({
         subject: Type.String(),
         validFrom: Type.String(),
